# runtime state & secrets — never commit
.venv/
data/
logs/
.secrets/
*.db
*.db-wal
*.db-shm
__pycache__/
*.pyc
# host-specific live config (once Phase 1 lands, real values live here, not in code)
secwatch.yaml
.secwatch-canary-*
