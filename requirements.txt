# Pinned to a tested, known-good set so every install — today's and next year's —
# is reproducible. Without this, a new node could pull a newer fastapi/pydantic/
# starlette with a breaking change and fail while your existing nodes keep working.
# Bump these deliberately after testing a new set.
fastapi==0.139.0
uvicorn[standard]==0.51.0
starlette==1.3.1
pydantic==2.13.4
pyyaml==6.0.3
python-multipart==0.0.32
# cryptography: a floor (with an upper guard) rather than a hard pin — we WANT its
# security updates, and it keeps a stable API for the Fernet usage we rely on.
cryptography>=41,<50
